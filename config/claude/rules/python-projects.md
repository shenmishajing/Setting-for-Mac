# Python projects

Every Python project uses Pixi as its package manager unless it already declares pip or conda (`requirements.txt`, `environment.yml`). Detailed Python conventions are in `python.md` next to this file and load when Python files are touched.
